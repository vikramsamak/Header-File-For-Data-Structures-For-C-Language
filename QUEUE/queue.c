#include<stdio.h>
#include"queue.h"
void main()
{
    queuedatastruct(10);
    enqueue(1);
    enqueue(2);
    enqueue(3);
    enqueue(4);
    enqueue(5);
    dequeue();
    displayqueue();
    searchfromqueue(5);
    
}